# Telegram Mini App - Инструкция по настройке

## Что такое Mini App?

Mini App - это веб-приложение, которое открывается прямо внутри Telegram. Оно содержит подробные инструкции по настройке VPN для разных платформ и ссылки на программы для подключения.

## Структура проекта

```
bot/
└── webapp/
    ├── __init__.py           # Инициализация модуля
    ├── server.py             # Веб-сервер на aiohttp
    ├── templates/
    │   └── index.html        # Главная страница mini app
    └── static/
        ├── css/
        │   └── style.css     # Стили приложения
        └── js/
            └── app.js        # JavaScript функционал
```

## Настройка

### 1. Настройка переменных окружения

Добавьте в ваш `.env` файл следующие переменные:

```env
# WebApp (Mini App) Configuration
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=8080
WEBAPP_URL=https://ваш-домен.ru:8080
```

**Важно:**
- `WEBAPP_URL` должен быть HTTPS URL, доступный из интернета
- Telegram требует HTTPS для mini apps (не работает с HTTP)
- Используйте ваш реальный домен вместо `ваш-домен.ru`

### 2. Настройка HTTPS (обязательно!)

Telegram Mini Apps работают только по HTTPS. Вам нужно настроить SSL сертификат:

#### Вариант 1: Nginx с Let's Encrypt (рекомендуется)

1. Установите Nginx:
```bash
apt update
apt install nginx certbot python3-certbot-nginx
```

2. Создайте конфигурацию Nginx:
```nginx
server {
    listen 443 ssl http2;
    server_name ваш-домен.ru;

    ssl_certificate /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ваш-домен.ru/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

3. Получите SSL сертификат:
```bash
certbot --nginx -d ваш-домен.ru
```

#### Вариант 2: Использование внешнего reverse proxy

Если у вас уже есть reverse proxy (Cloudflare, nginx на другом сервере и т.д.), настройте проксирование на порт 8080.

### 3. Запуск бота

После настройки запустите бота обычным способом:

```bash
python3 main.py
```

В логах вы должны увидеть:
```
WebApp сервер запущен успешно
WebApp server started on http://0.0.0.0:8080
```

### 4. Проверка работы

1. Откройте бота в Telegram
2. Нажмите кнопку "📖 Инструкции" в главном меню
3. Должно открыться веб-приложение с инструкциями

## Функции Mini App

### 📖 Вкладка "Инструкции"

Содержит пошаговые инструкции по настройке VPN для:
- iOS (iPhone/iPad)
- Android
- Windows
- macOS

Каждая инструкция включает:
- Список необходимых приложений
- Пошаговые действия
- Подсказки и советы

### 📱 Вкладка "Программы"

Содержит прямые ссылки на скачивание приложений:
- v2rayNG для iOS
- Shadowrocket для iOS
- v2rayNG для Android (Google Play и APK)
- v2rayN для Windows
- v2rayU для macOS
- Hiddify (кроссплатформенное решение)

## Система уведомлений для менеджеров

Добавлена новая функция для администратора - отправка уведомлений всем менеджерам.

### Как использовать:

1. Откройте "Панель администратора"
2. Нажмите "📢 Отправить уведомление"
3. Введите текст уведомления
4. Сообщение будет отправлено всем менеджерам

### Возможности:

- Поддержка HTML-форматирования (жирный текст, курсив, ссылки)
- Автоматический отчет о статусе доставки
- Список менеджеров, которым не удалось доставить сообщение

### Примеры использования:

- Уведомление о новых инструкциях в mini app
- Информирование об изменении цен
- Важные объявления и обновления
- Технические работы или простои

## Кастомизация Mini App

### Изменение контента

Все тексты и инструкции находятся в файле:
```
bot/webapp/templates/index.html
```

Вы можете редактировать:
- Список платформ
- Инструкции по настройке
- Ссылки на приложения
- Советы и рекомендации

### Изменение дизайна

Стили приложения находятся в:
```
bot/webapp/static/css/style.css
```

Mini App автоматически адаптируется под тему Telegram (светлую/темную).

### Добавление новых страниц

Вы можете добавить новые вкладки, отредактировав:
1. HTML-структуру в `index.html`
2. CSS-стили в `style.css`
3. JavaScript логику в `app.js`

## Решение проблем

### Mini App не открывается

1. Проверьте, что `WEBAPP_URL` в `.env` указан правильно
2. Убедитесь, что используется HTTPS (HTTP не работает!)
3. Проверьте, что порт 8080 открыт в файрволе
4. Проверьте логи бота на наличие ошибок

### Кнопка "Инструкции" не появляется

1. Перезапустите бота
2. Отправьте команду `/start` в боте
3. Проверьте, что `WEBAPP_URL` настроен в `.env`

### Не приходят уведомления менеджерам

1. Проверьте, что менеджеры запустили бота (`/start`)
2. Проверьте логи на наличие ошибок
3. Убедитесь, что у бота есть права на отправку сообщений

## Безопасность

- Mini App работает только по HTTPS
- Используется официальный Telegram Web App API
- Не требует дополнительной аутентификации (авторизация через Telegram)
- Все данные передаются через защищенное соединение

## Обновление Mini App

Для обновления контента:
1. Отредактируйте нужные файлы в `bot/webapp/`
2. Перезапустите бота
3. Отправьте уведомление менеджерам о новых инструкциях

Изменения применятся сразу для всех пользователей!

## Поддержка

Если возникли проблемы:
1. Проверьте логи бота (`bot.log`)
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки `.env` файла
