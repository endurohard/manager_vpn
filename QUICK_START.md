# Быстрый старт Mini App

## 🚀 Что нужно сделать

### 1. Обновите файл .env

Добавьте эти строки в ваш `.env` файл:

```env
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=8080
WEBAPP_URL=https://raphaelvpn.ru:8080
```

⚠️ **Замените `raphaelvpn.ru` на ваш реальный домен!**

### 2. Настройте HTTPS

**Вариант A: Nginx + Let's Encrypt (рекомендуется)**

```bash
# Установка
apt update
apt install nginx certbot python3-certbot-nginx

# Создайте файл /etc/nginx/sites-available/miniapp
nano /etc/nginx/sites-available/miniapp
```

Содержимое файла:
```nginx
server {
    listen 80;
    server_name raphaelvpn.ru;  # ВАШ ДОМЕН!
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name raphaelvpn.ru;  # ВАШ ДОМЕН!

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
# Активация конфига
ln -s /etc/nginx/sites-available/miniapp /etc/nginx/sites-enabled/
nginx -t

# Получение SSL сертификата
certbot --nginx -d raphaelvpn.ru  # ВАШ ДОМЕН!

# Перезапуск nginx
systemctl restart nginx
```

**Вариант B: Cloudflare**

1. Добавьте A-запись для вашего домена на IP сервера
2. Включите SSL/TLS в Cloudflare (режим "Full")
3. Mini App будет работать через Cloudflare SSL

### 3. Откройте порт 8080

```bash
# UFW
ufw allow 8080/tcp

# iptables
iptables -A INPUT -p tcp --dport 8080 -j ACCEPT
```

### 4. Запустите бота

```bash
cd /root/manager_vpn
python3 main.py
```

В логах должно появиться:
```
✓ WebApp сервер запущен успешно
✓ WebApp server started on http://0.0.0.0:8080
```

### 5. Проверьте работу

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "📖 Инструкции"
4. Должно открыться веб-приложение!

## 📢 Отправка уведомлений менеджерам

1. Панель администратора
2. "📢 Отправить уведомление"
3. Введите текст
4. Готово!

**Пример:**
```
Добавлены новые инструкции! 📖

Проверьте раздел "Инструкции" в боте.
Теперь доступны подробные гайды для всех платформ.
```

## 🔧 Если что-то не работает

**Mini App не открывается:**
- Проверьте, что домен правильный в `.env`
- Убедитесь, что используется HTTPS (не HTTP!)
- Проверьте SSL сертификат
- Посмотрите логи: `tail -f bot.log`

**Кнопка не появляется:**
- Перезапустите бота
- Отправьте `/start` заново
- Проверьте `.env` файл

**Нужна помощь?**
- Читайте `MINIAPP_SETUP.md` - подробная инструкция
- Читайте `MINIAPP_CHANGELOG.md` - список всех изменений
- Проверяйте `bot.log` - логи бота

## ✅ Готово!

После настройки:
- ✅ Менеджеры смогут открывать инструкции прямо в боте
- ✅ Вы сможете отправлять уведомления всем менеджерам
- ✅ Все автоматически работает и обновляется
