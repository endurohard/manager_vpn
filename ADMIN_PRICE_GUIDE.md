# 💰 Руководство по изменению цен для администратора

## Как изменить цены в боте

### Способ 1: Через Telegram бота (рекомендуется) ⭐

1. Откройте бот @raphaelvpn_bot
2. Нажмите **"Панель администратора"**
3. Нажмите **"💰 Изменить цены"**
4. Выберите тариф для изменения
5. Введите новую цену (только число)
6. Получите подтверждение

**Преимущества:**
- ✅ Простой и удобный интерфейс
- ✅ Мгновенное применение изменений
- ✅ Показывает старую и новую цену
- ✅ Защита от некорректного ввода
- ✅ Не требует перезапуска бота

### Способ 2: Через файл конфигурации

Если нужно изменить цены вручную:

1. Откройте файл `prices.json`:
```bash
nano /root/manager_vpn/prices.json
```

2. Измените цены:
```json
{
  "1_month": {
    "name": "Месяц",
    "days": 30,
    "price": 300      ← изменить здесь
  },
  "3_months": {
    "name": "3 месяца",
    "days": 90,
    "price": 800      ← изменить здесь
  },
  "6_months": {
    "name": "6 месяцев",
    "days": 180,
    "price": 1500     ← изменить здесь
  },
  "1_year": {
    "name": "Год",
    "days": 365,
    "price": 2500     ← изменить здесь
  }
}
```

3. Сохраните файл (Ctrl+O, Enter, Ctrl+X)

4. **Перезапустите бота:**
```bash
pkill -f "python3 main.py"
sleep 2
cd /root/manager_vpn
source venv/bin/activate
nohup python3 main.py > bot_output.log 2>&1 &
```

## Текущие цены

| Тариф | Дней | Цена | Цена/день |
|-------|------|------|-----------|
| Месяц | 30 | 300 ₽ | 10.0 ₽ |
| 3 месяца | 90 | 800 ₽ | 8.9 ₽ |
| 6 месяцев | 180 | 1,500 ₽ | 8.3 ₽ |
| Год | 365 | 2,500 ₽ | 6.8 ₽ |

## Где обновляются цены

После изменения цены автоматически обновляются:

1. **Прайс-лист** ("💰 Прайс")
   - Показывает новые цены
   - Пересчитывает цену за день

2. **Меню создания ключа**
   - Кнопки выбора тарифа показывают новые цены
   - Например: "Месяц - 350 ₽"

3. **Подтверждение создания**
   - Показывает актуальную цену выбранного тарифа

4. **Статистика**
   - Новые ключи записываются с новыми ценами
   - Старые ключи сохраняют свои цены

5. **Панель администратора**
   - "💰 Изменить цены" показывает актуальные цены

## Важные моменты

⚠️ **Старые ключи:**
- Сохраняют цены, по которым были созданы
- Статистика доходов учитывает реальные цены создания

⚠️ **Новые ключи:**
- Создаются с новыми ценами
- Автоматически используют актуальный прайс

⚠️ **Перезапуск бота:**
- Через Telegram: перезапуск не нужен
- Через файл: требуется перезапуск

## Проверка изменений

После изменения цены проверьте:

1. Нажмите "💰 Прайс" - должны быть новые цены
2. Начните создавать ключ - должны быть новые цены в кнопках
3. Проверьте логи: `tail -20 bot_output.log`

## Отмена изменений

Если нужно вернуть цены:

1. Через бот: снова измените цену на старую
2. Через файл: замените значения и перезапустите

## Резервная копия

Перед изменением цен рекомендуется создать копию:

```bash
cp /root/manager_vpn/prices.json /root/manager_vpn/prices.json.backup
```

Восстановление:
```bash
cp /root/manager_vpn/prices.json.backup /root/manager_vpn/prices.json
pkill -f "python3 main.py" && sleep 2
cd /root/manager_vpn && source venv/bin/activate
nohup python3 main.py > bot_output.log 2>&1 &
```

## Файлы системы цен

- `prices.json` - текущие цены (редактируется ботом)
- `bot/price_config.py` - менеджер цен
- `bot/handlers/admin.py` - обработчики изменения цен
- `bot/database/db_manager.py` - сохранение цен в историю

## Логи изменений

Все изменения цен логируются в консоли бота.

Посмотреть логи:
```bash
tail -50 /root/manager_vpn/bot_output.log
```

## Поддержка

При проблемах с изменением цен:

1. Проверьте права доступа к `prices.json`:
   ```bash
   ls -la /root/manager_vpn/prices.json
   ```

2. Проверьте что бот запущен:
   ```bash
   ps aux | grep "python3 main.py"
   ```

3. Проверьте логи на ошибки:
   ```bash
   tail -50 bot_output.log | grep -i error
   ```

4. Перезапустите бота:
   ```bash
   pkill -f "python3 main.py" && sleep 2
   cd /root/manager_vpn && source venv/bin/activate
   nohup python3 main.py > bot_output.log 2>&1 &
   ```
